
from setuptools import setup, find_packages

setup(
    name="{{project_name}}",
    version="",
    url='',
    license='',
    description="",
    author='',
    packages=find_packages('src'),
    package_dir={'': 'src'},
    install_requires=['setuptools'],
)
