[buildout]
extends = buildout.cfg

parts += dev 
eggs += ${dev:eggs}

[django]

extends = buildout.cfgu
settings = {{project_name}}.settings.dev

[dev]
recipe = zc.recipe.egg
extra-paths = ${buildout:directory}/
eggs = 
  ipython
  Sphinx
  Werkzeug
  django-debug-toolbar


